import { FC } from "react";

export interface TradesManProfileProps {
  tradesmanId: string;
}

const TradesmanProfile: FC<TradesManProfileProps> = ({ tradesmanId }) => {
  return <></>;
};

export default TradesmanProfile;
